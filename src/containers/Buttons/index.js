import styled from 'styled-components'
import { bg, fg } from '../../colors'

export const Button = styled.button`
background-color: ${fg.translucid};
color: ${fg.dark};
outline-style: none;
border: none;
cursor: pointer;
padding: 0.5rem;

`
